import Card from "@/components/ui/card/Card"

interface Props{
    mode: string
}

export default function AllStandards({mode}: Props) {
    return (
        <Card>
            hell i card
        </Card>
    )
}
